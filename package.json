{
  "name": "small-bule-note",
  "version": "1.0.0",
  "description": "WeChat-like IM App",
  "private": true,
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "cd backend && npm ci && npm run build",
    "start": "cd backend && NODE_ENV=production npm run start:prod"
  }
}
